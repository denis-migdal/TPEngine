Question shared structure:
- `<q-[type] @qid="#[id]" @coeff="[coeff]">[question]</q-text>`.

Specific properties:
- `<q-text>` : `@lang`.
- `<q-multitext>` : `@nb-fields`, `@nb-cols`.
- `<q-file>` : `@accept` (file extension, e.g. `.svg`).