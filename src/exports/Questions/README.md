Question shared structure:
- `<q-[type] cfg-qid="#[id]" cfg-coeff="[coeff]">[question]</q-text>`.

Specific properties:
- `<q-text>` : `cfg-lang`.
- `<q-multitext>` : `cfg-nb-fields`, `cfg-nb-cols`.
- `<q-file>` : `cfg-accept` (file extension, e.g. `.svg`).